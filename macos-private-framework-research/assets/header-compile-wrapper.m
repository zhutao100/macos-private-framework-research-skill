#import <Foundation/Foundation.h>

// Replace this include path with the generated header under validation.
// The linter can create a temporary wrapper automatically when run with --compile.
#import "GeneratedPrivateFrameworkHeader.h"

int main(int argc, const char * argv[]) {
    @autoreleasepool {
        (void)argc;
        (void)argv;
    }
    return 0;
}
